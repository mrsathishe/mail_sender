# Secrets & local env — never commit
.env
.env.*
!.env.example

# Dependencies & build output
node_modules
.next
out
build
coverage

# OS / editor noise
.DS_Store
*.log

# Kubernetes real secret (use secret.example.yaml as a template)
k8s/secret.yaml
